# DenoiseRL 近期改进与后续计划

## 1. 动态控制机制

近期，我们在 DenoiseRL 中引入了动态控制机制，不再为所有训练样本固定使用同一个噪声比例 \(\rho\)，而是为每个样本单独维护和调整 \(\rho\)。其中，\(\rho\) 表示保留错误推理前缀的比例；\(\rho\) 越大，模型需要从越长的错误前缀中恢复，任务也越困难。

对于每个样本，我们根据当前 rollout 的平均正确率动态更新 \(\rho\)：

\[
\rho_i \leftarrow \operatorname{clip}\left(\rho_i + \alpha(A_i-A_{\text{target}}),\rho_{\min},\rho_{\max}\right)
\]

当样本的正确率高于目标值时，增大 \(\rho\)，为该样本加入更强的噪声；当正确率低于目标值时，减小 \(\rho\)，降低恢复难度。系统还会记录每个样本最近一段时间的 \(\rho\) 变化。当变化趋势趋于稳定时，该样本会被新的训练样本替换，新样本继承当前 batch 更新后的平均 \(\rho\)。因此，这一过程可以看作一种按样本难度自适应的 curriculum learning。

该机制的主要优点是控制更加细粒度：不同样本可以根据自身难度获得不同强度的噪声，避免统一的 \(\rho\) 对简单样本过弱、对困难样本过强。

它的主要缺点是训练多样性可能下降。一个样本在达到稳定状态前会被重复采样和训练，因此短时间内使用的训练样本数量有限，容易出现样本重复，并可能带来一定程度的过拟合。

## 2. 补充实验

在 rebuttal 期间，我们已经补充了以下实验：

- **ALFWorld 实验：** 在 ALFWorld 上测试适配后的动态控制方案，用于观察该机制能否从数学推理任务迁移到交互式决策和 agent 场景。ALFWorld 的轨迹由离散的交互步骤组成，因此不能像数学推理一样按 token 截断错误 prefix，而需要按照 step 切分轨迹，并以保留的 step 比例控制扰动强度。
- **ALFWorld 的在线扰动：** 在 ALFWorld 中离线构造和维护错误轨迹较为麻烦，因此没有直接复用数学任务中的离线错误 prefix，而是在 rollout 过程中采用在线扰动（online perturbation）的方式动态加入干扰。
- **不同 noise 来源：** 在数学任务上，对比 **Pre-RL Self**（RL 前的模型自身生成）、**Small Model**（小模型生成）和 **Random Token**（随机 token）三种噪声来源，以分析性能变化究竟来自结构化的错误推理，还是仅仅来自一般性的输入扰动。

## 3. 后续判断与计划

动态控制在直觉上能够让噪声难度更贴合模型当前能力，但它并不一定会带来稳定的性能提升。后续将重点比较动态控制与固定 \(\rho=0.2\) 方案，并尽量保持训练数据、计算量和评测设置一致。

如果动态控制没有取得稳定且可复现的收益，我们将继续采用更简单、稳定的固定 \(\rho=0.2\) 方案。ALFWorld 的 step-level 在线扰动实验和数学任务上的不同 noise 来源实验已经在 rebuttal 期间补充完成，接下来主要根据实验结果判断动态控制是否值得保留。
